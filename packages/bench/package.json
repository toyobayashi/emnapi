{
  "name": "@emnapi/bench",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "rebuild": "emcmake cmake -DCMAKE_BUILD_TYPE=Release -H. -B.build && cmake --build .build",
  },
  "devDependencies": {
    "node-addon-api": "6.0.0",
    "benchmark": "^2.1.4"
  }
}
