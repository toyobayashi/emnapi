{
  "name": "@tybys/emnapi",
  "main": "cgen.config.js",
  "scripts": {
    "configure": "cgen configure -e -d",
    "build": "cgen build -d",
    "clean": "cgen clean",
    "rebuild": "tsc -p ./lib/runtime/tsconfig.json && tsc && node ./script/build.js && cgen rebuild -e -d",
    "rebuild:r": "tsc -p ./lib/runtime/tsconfig.json && tsc && node ./script/build.js && cgen rebuild -e"
  },
  "devDependencies": {
    "tslib": "^2.2.0",
    "typescript": "^4.2.4"
  }
}
